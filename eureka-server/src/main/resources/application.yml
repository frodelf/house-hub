server:
  port: 7071
spring:
  application:
    name: eserver
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:7071/eureka
logging:
  level:
    com:
      netflix:
        eureka: off
        discovery: off